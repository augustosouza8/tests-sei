# Variáveis de ambiente para o projeto SEI
# Copie este arquivo para .env e preencha com suas credenciais reais
# 
# IMPORTANTE: O arquivo .env não deve ser commitado no repositório (já está no .gitignore)

# ============================================================================
# CREDENCIAIS OBRIGATÓRIAS
# ============================================================================

# Login do SEI
SEI_USER=seu_login_aqui

# Senha do SEI
SEI_PASS=sua_senha_aqui

# Código do órgão SEI (ex: 28 para SEPLAG, 52 para FHEMIG)
SEI_ORGAO=28

# Nome da unidade SEI desejada (deve corresponder exatamente ao nome no sistema)
# Para descobrir o nome exato: faça login no SEI e clique no link de unidade no topo
# Exemplos: "SEPLAG/AUTOMATIZAMG", "FHEMIG/DIRASS/GEPI/CIP/CFA"
SEI_UNIDADE=SEPLAG/AUTOMATIZAMG

# ============================================================================
# CONFIGURAÇÕES OPCIONAIS
# ============================================================================

# Ativa logs detalhados (1 para ativar, 0 ou vazio para desativar)
SEI_DEBUG=1

# Salva HTMLs de debug em data/debug/ (1 para ativar)
# Útil para auditoria ou troubleshooting; desative em produção
SEI_SAVE_DEBUG_HTML=1

# Diretório base para dados persistentes (histórico, iframes, etc.)
# SEI_DATA_DIR=data

# ============================================================================
# FILTROS OPCIONAIS (podem ser usados via CLI também)
# ============================================================================

# Filtro de visualização: "visualizados" ou "nao_visualizados"
# SEI_FILTRO_VISUALIZACAO=nao_visualizados

# Categorias: "recebidos", "gerados" ou ambos separados por vírgula
# SEI_FILTRO_CATEGORIA=recebidos

# Responsáveis (substring, separados por vírgula)
# SEI_FILTRO_RESPONSAVEL=João, Maria

# Tipos/Especificidade (substring, separados por vírgula)
# SEI_FILTRO_TIPO=Estágio, Viagem

# Marcadores (substring, separados por vírgula)
# SEI_FILTRO_MARCADOR=assinatura, simplificação

# Limite máximo de processos após aplicar filtros
# SEI_FILTRO_LIMITE=10

# ============================================================================
# EXPORTAÇÃO E HISTÓRICO
# ============================================================================

# Caminho para exportar automaticamente para Excel
# SEI_EXPORTAR_XLSX=saida/processos.xlsx

# Ativa salvamento de histórico em JSON (1 para ativar)
# SEI_SALVAR_HISTORICO=1

# Caminho do arquivo de histórico (default: data/historico_processos.json)
# SEI_HISTORICO_ARQUIVO=data/historico_processos.json

# ============================================================================
# COLETA DE DOCUMENTOS
# ============================================================================

# Ativa coleta de metadados de documentos do iframe (1 para ativar)
# SEI_COLETAR_DOCUMENTOS=1

# Limite de processos para coleta detalhada de documentos
# SEI_LIMITE_PROCESSOS_DOCUMENTOS=5

# Salva HTMLs dos iframes em disco (1 para ativar)
# SEI_DUMP_IFRAMES=1

# Limite de iframes salvos quando dump está ativo
# SEI_DUMP_IFRAMES_LIMITE=5

# Diretório para salvar iframes (default: data/iframes)
# SEI_DUMP_IFRAMES_DIR=data/iframes

# ============================================================================
# PAGINAÇÃO
# ============================================================================

# Máximo de páginas a carregar de Recebidos
# SEI_PAGINAS_RECEBIDOS=10

# Máximo de páginas a carregar de Gerados
# SEI_PAGINAS_GERADOS=10

# Limite geral de páginas (aplica a ambas as categorias)
# SEI_PAGINAS_MAX=5

# ============================================================================
# DOWNLOAD DE PDFs EM LOTE
# ============================================================================

# Ativa download em lote de PDFs (1 para ativar)
# SEI_DOWNLOAD_LOTE=1

# Limite de processos para download em lote
# SEI_MAX_PROCESSOS_PDF=10

# Diretório de saída dos PDFs gerados (default: diretório atual)
# SEI_PDF_DIR=./pdfs

# Habilita processamento paralelo (1 para ativar)
# SEI_PDF_PARALELO=1

# Número de workers no modo paralelo (default: 3)
# SEI_PDF_WORKERS=3

# Número de tentativas por processo (default: 3)
# SEI_PDF_RETRIES=3
